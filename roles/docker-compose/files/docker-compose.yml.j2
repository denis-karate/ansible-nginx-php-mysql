version: '3.8'

services:
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    ports:
      - "88:88"

  php:
    build:
      context: ./php
      dockerfile: Dockerfile

  mysql:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: "{{ mysql_root_password }}"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:

